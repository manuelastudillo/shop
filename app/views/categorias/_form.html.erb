<div class="container-fluid">
 <div class="row">
  <div class="panel-body">
   <%= form_for(categoria) do |f| %>
    <% if categoria.errors.any? %>
     <div id="error_explanation">
      <h2><%= pluralize(categoria.errors.count, "error") %> prohibited this categoria from being saved:</h2>

      <ul>
       <% categoria.errors.full_messages.each do |message| %>
        <li><%= message %></li>
       <% end %>
      </ul>
     </div>
    <% end %>

    <div class="form-group">
     <%= f.text_field :nombre, autofocus: true, placeholder: "Nombre", class: "form-control" %>
    </div>
        <div class="form-group">
     <%= f.text_area :descripcion, autofocus: true, placeholder: "Descripcion", class: "form-control" %>
    </div>

    <%= f.submit "Aceptar", class: "btn btn-success" %>
   <% end %>
  </div>
 </div>
</div>